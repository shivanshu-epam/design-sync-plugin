# Node
*.log
*.log.*
node_modules

out/
dist/
code.js
ui.html

# Local Claude Code tool state (permissions, dev-server launch config with
# machine-specific absolute paths) — not portable project source. Skills
# ARE portable project source (no machine-specific state), so they're
# carved out and tracked.
.claude/*
!.claude/skills/
